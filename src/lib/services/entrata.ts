export const EntrataService = {
    // Placeholder for future API calls (e.g., getting resident details)
    async getResidentDetails(residentId: string) {
        return {
            id: residentId,
            name: 'Mock Resident',
            email: 'mock.resident@example.com'
        };
    }
};
